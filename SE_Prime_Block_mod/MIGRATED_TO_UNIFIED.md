This folder is **legacy**. New worlds should use **[SE_Unified_mod](../SE_Unified_mod/)** instead of subscribing to both `SE_Upgrade_module_mod` and `SE_Prime_Block_mod`.

Prefab **Prime Matter** cargo rolls and economy data ship inside the unified mod; this package alone is superseded by the unified layout.

Do not enable this mod together with **SE_Unified_mod**.
